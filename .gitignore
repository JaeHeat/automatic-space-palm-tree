# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Data cache (re-fetched from FRED on demand). Keep the dir, ignore CSVs.
data/cache/*.csv
!data/cache/.gitkeep

# Generated charts (re-created by the scripts). Comment out to commit them.
outputs/*.png
!outputs/.gitkeep
